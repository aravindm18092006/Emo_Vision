services:
  - type: web
    name: emo-vision
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:10000 --timeout 120 --workers 1 --threads 1
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
